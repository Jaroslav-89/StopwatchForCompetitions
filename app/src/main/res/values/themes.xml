<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.StopwatchForCompetitions" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.StopwatchForCompetitions" parent="Base.Theme.StopwatchForCompetitions">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white_day_night</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="trackTint">@color/track_day_night</item>
        <item name="thumbTint">@color/thumb_day_night</item>
    </style>
</resources>