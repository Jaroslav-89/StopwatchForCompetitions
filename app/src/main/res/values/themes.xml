<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.StopwatchForCompetitions" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="Theme.StopwatchForCompetitions" parent="Base.Theme.StopwatchForCompetitions">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>